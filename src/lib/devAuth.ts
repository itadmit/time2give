/**
 * קבועים לדיבוג בלבד (משמשים רק כאשר __DEV__).
 * אם תבחר במסלול "מספר בדיקה" - הגדר את אותו מספר+קוד ב-Supabase:
 * Dashboard → Authentication → Providers → Phone → Test phone numbers.
 */
export const DEV_TEST_PHONE = '+972500000000';
export const DEV_TEST_CODE = '123456';
